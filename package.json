{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "setup": "node scripts/setup-env.mjs --docker && pnpm install && pnpm db:up && node scripts/wait-for-postgres.mjs && pnpm --filter @workspace/db run push",
    "setup:env": "node scripts/setup-env.mjs",
    "db:up": "docker compose -f infra/docker-compose.dev.yml up -d postgres",
    "db:down": "docker compose -f infra/docker-compose.dev.yml stop postgres",
    "dev": "concurrently -k -n api,web -c blue,green \"pnpm --filter @workspace/api-server run dev\" \"pnpm --filter @workspace/web run dev\"",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:web": "pnpm --filter @workspace/web run dev",
    "verify": "pnpm run typecheck && pnpm test",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm --filter @workspace/api-server test",
    "smoke:invite": "node scripts/invite-flow-smoke.mjs",
    "smoke:features": "node scripts/features-smoke.mjs"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "pnpm": {
    "overrides": {
      "js-yaml": "4.1.1"
    }
  }
}
